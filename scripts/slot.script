go.property("slot_index", 0)
go.property("balls_count", 0)

local BALL_FELL = hash("ball_fell")

function on_message(self, message_id, message, sender)
	if message_id == BALL_FELL then
		self.balls_count = self.balls_count + math.max(1, math.floor(message.count or 1))
	end
end
